#!/bin/bash

# 指定作业名，此脚本默认m文件为$LSB_JOBNAME.m
#BSUB -J test

# 指定计算核数
#BSUB -n 24

# 指定队列，通过命令bqueues获得队列名称
#BSUB -q E5-2678v3

# 用户自定义命令，添加在主程序末尾
ADD_ARGS=""

####################################################################################
#   如果以下内容不懂的话，不要轻易修改，（有更简洁的写法，但这里是为了方便管理员debug用） #
####################################################################################
#BSUB -o %J.out
#BSUB -e %J.err

matlab -nodisplay -r $LSB_JOBNAME -logfile $LSB_JOBNAME.log $ADD_ARGS
